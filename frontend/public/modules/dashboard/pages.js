/**
 * Dashboard module pages stub.
 * Live dashboard UI is rendered by greenos-dashboard.js — do not duplicate here.
 */
window.GreenOSModules = window.GreenOSModules || {};
window.GreenOSModules['dashboard'] = {
  render(root, subPageId) {
    // Dashboard is rendered by greenos-dashboard.js
    if (root) {
      root.innerHTML = '<!-- Dashboard rendered by greenos-dashboard.js -->';
    }
  },
};
