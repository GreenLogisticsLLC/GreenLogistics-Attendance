/**
 * AI module pages stub.
 * AI chat UI is rendered by greenos-ai.js — do not duplicate here.
 */
window.GreenOSModules = window.GreenOSModules || {};
window.GreenOSModules['ai'] = {
  render(root, subPageId) {
    // AI is rendered by greenos-ai.js
    if (root) {
      root.innerHTML = '<!-- AI rendered by greenos-ai.js -->';
    }
  },
};
